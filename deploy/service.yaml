apiVersion: v1
kind: Service
metadata:
  name: schip-spinnaker-webhook
  labels:
    app: schip-spinnaker-webhook
spec:
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app: schip-spinnaker-webhook
